# Copia este archivo a ".env" y pon tus credenciales reales.
MONGODB_URI=mongodb+srv://USUARIO:CONTRASENA@TUCLUSTER.mongodb.net/?appName=DevWeb
MONGODB_DB=devweb
MONGODB_DNS_SERVERS=8.8.8.8,1.1.1.1

# Front: origenes permitidos por CORS (separados por coma) y hosts del backend
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
ALLOWED_HOSTS=127.0.0.1,localhost
